<Window x:Class="qlks_app.views.Login_View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:qlks_app.view_model"
        mc:Ignorable="d"
        Title="Login_View" Height="450" Width="800">

    <Window.DataContext>
        <local:Login_ViewModel/>
    </Window.DataContext>
    
    <Grid Background="#f0f0f0">
        <Border Background="White" 
                CornerRadius="10" 
                Padding="20"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">

            <StackPanel>
                <!-- Title -->
                <TextBlock Text="ĐĂNG NHẬP"
                           FontSize="20"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <!-- Username -->
                <TextBlock Text="Tên tài khoản"/>
                <TextBox Text="{Binding tenTaiKhoan}" Height="30" Margin="0,5,0,10"/>

                <!-- Password -->
                <TextBlock Text="Mật khẩu"/>
                <TextBox Text="{Binding matKhau, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,5,0,15"/>

                <!-- Role Selection -->
                <TextBlock Text="Đăng nhập với vai trò"/>
                <ComboBox Height="30" Margin="0,5,0,15" SelectedValue="{Binding vaiTro}" SelectedValuePath="Content">
                    <ComboBoxItem Content="Quản trị"/>
                    <ComboBoxItem Content="Khách hàng"/>
                </ComboBox>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Đăng nhập" Width="80" Margin="5" Command="{Binding DangNhapCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                    <Button Content="Hủy" Width="80" Margin="5" Command="{Binding HuyCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
