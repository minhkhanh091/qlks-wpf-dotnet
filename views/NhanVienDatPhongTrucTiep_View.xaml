<UserControl x:Class="qlks_app.views.NhanVienDatPhongTrucTiep_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#2C3E50"/>
        <SolidColorBrush x:Key="AccentColor" Color="#3498DB"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#27AE60"/>
        <SolidColorBrush x:Key="DangerColor" Color="#E74C3C"/>
        <SolidColorBrush x:Key="WarningColor" Color="#F39C12"/>
        <SolidColorBrush x:Key="BackgroundColor" Color="#ECF0F1"/>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SuccessColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#229954"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20">
            <Grid>
                <StackPanel>
                    <TextBlock Text="🧾 LỄ TÂN - ĐẶT PHÒNG" FontSize="22" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryColor}"/>
                    <TextBlock Text="Đặt phòng trực tiếp và xác nhận đặt phòng online"
                               FontSize="12" Foreground="#7F8C8D" Margin="0,5,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Style="{StaticResource SuccessButton}" Width="110"
                            Command="{Binding TaiLaiCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔄 " FontSize="14"/>
                            <TextBlock Text="Làm mới" FontSize="13"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource PrimaryButton}" Width="100"
                            Command="{Binding DongCommand}"
                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="❌ " FontSize="14"/>
                            <TextBlock Text="Đóng" FontSize="13"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20">
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="340"/>
                </Grid.ColumnDefinitions>

                <DataGrid Grid.Column="0" AutoGenerateColumns="False" SelectionMode="Single"
                          SelectionUnit="FullRow" IsReadOnly="True" AlternatingRowBackground="#F9F9F9"
                          RowHeight="40" FontSize="13"
                          ItemsSource="{Binding DanhSachPhongTrong}"
                          SelectedItem="{Binding PhongDangChon, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Số phòng" Binding="{Binding SoPhong}" Width="90"/>
                        <DataGridTextColumn Header="Loại phòng" Binding="{Binding LoaiPhong.TenLoai}" Width="140"/>
                        <DataGridTextColumn Header="Giá/đêm" Binding="{Binding GiaMoiDem, StringFormat=N0}" Width="120"/>
                        <DataGridTextColumn Header="Sức chứa" Binding="{Binding SucChua}" Width="90"/>
                        <DataGridTextColumn Header="Mô tả" Binding="{Binding MoTa}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Column="1" Margin="15,0,0,0">
                    <Button Style="{StaticResource SuccessButton}" Width="120"
                            Command="{Binding MoDatTrucTiepCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🛎️ " FontSize="14"/>
                            <TextBlock Text="Đặt phòng" FontSize="13"/>
                        </StackPanel>
                    </Button>

                    <Border Background="#F8F9FA" CornerRadius="8" Padding="15" Margin="0,10,0,0"
                            BorderBrush="#E0E0E0" BorderThickness="1"
                            Visibility="{Binding HienThiFormDatTrucTiep, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="110"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Phòng:" FontWeight="Bold"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding PhongDangChon.SoPhong}"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Ngày checkin:" FontWeight="Bold" Margin="0,10,0,0"/>
                            <DatePicker Grid.Row="1" Grid.Column="1" SelectedDate="{Binding NgayCheckin}" Margin="0,10,0,0"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Số đêm:" FontWeight="Bold" Margin="0,10,0,0"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SoDem, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,0"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Họ tên:" FontWeight="Bold" Margin="0,10,0,0"/>
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding HoTen, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,0"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Điện thoại:" FontWeight="Bold" Margin="0,10,0,0"/>
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding DienThoai, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,0"/>

                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Địa chỉ:" FontWeight="Bold" Margin="0,10,0,0"/>
                            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding DiaChi, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,0"/>

                            <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal"
                                        HorizontalAlignment="Center" Margin="0,15,0,0">
                                <Button Style="{StaticResource SuccessButton}" Width="100"
                                        Command="{Binding LuuDatTrucTiepCommand}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="💾 " FontSize="14"/>
                                        <TextBlock Text="Lưu" FontSize="13"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource PrimaryButton}" Width="100"
                                        Command="{Binding HuyDatTrucTiepCommand}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="❌ " FontSize="14"/>
                                        <TextBlock Text="Hủy" FontSize="13"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
